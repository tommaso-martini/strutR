% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/et.R
\name{mean_theta_in_root_zone}
\alias{mean_theta_in_root_zone}
\title{Mean water content in the root zone \eqn{\bar{\theta}(0\!-\!Z_{\mathrm{root}})}}
\usage{
mean_theta_in_root_zone(fronts, Z_root, theta_r, delta = 0)
}
\arguments{
\item{fronts}{list of fronts \code{list(list(theta=..., x=...), ...)}.}

\item{Z_root}{numeric; root-zone depth \link{m} from \code{delta}.}

\item{theta_r}{numeric; residual water content.}

\item{delta}{numeric; absolute top reference \link{m}.}
}
\value{
numeric scalar \eqn{\bar{\theta}} over \eqn{[0,Z_{\mathrm{root}}]}.
Returns \code{NA} if \eqn{Z_root \le 0}.
}
\description{
Computes the thickness-weighted mean \eqn{\bar{\theta}} within the top root-zone
depth \eqn{Z_{\mathrm{root}}}, defaulting uncovered spans to \eqn{\theta_r}.
This is consistent with the stepwise representation used in the ET operators.
}
\examples{
mean_theta_in_root_zone(list(list(theta=0.20,x=0.3), list(theta=0.28,x=0.8)),
                        Z_root = 0.5, theta_r = 0.05)
}
