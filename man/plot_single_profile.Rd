% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_single_profile}
\alias{plot_single_profile}
\title{Plot a stepwise θ(z) profile with fixed axes}
\usage{
plot_single_profile(
  theta_r,
  theta_s,
  profile,
  soil_depth,
  timestep = NULL,
  Z_1 = NA,
  Z_2 = NA,
  colore = NULL
)
}
\arguments{
\item{theta_r, theta_s}{numeric scalars; residual and saturated water contents.}

\item{profile}{list of points \code{list(list(theta=..., x=...), ...)} ordered
by absolute depth \code{x} \link{m}.}

\item{soil_depth}{numeric; bottom of the plotted domain \link{m}.}

\item{timestep}{optional character; label shown in the plot title.}

\item{Z_1, Z_2}{optional numeric; reference depths \link{m} to be drawn as dashed lines.}

\item{colore}{character; line color (default "black").}
}
\value{
Invisibly returns \code{NULL}. Side effect: produces a base R plot.
}
\description{
Draws a step-plot of a piecewise-constant water content profile \eqn{\theta(z)}
over depth, using fixed \eqn{[\theta_r,\theta_s]} on the x-axis and an inverted
y-axis (depth increases downward). Optional horizontal markers can be added
for \code{Z_1} and \code{Z_2}.
}
\examples{
prof <- list(list(theta=0.18,x=0.4), list(theta=0.25,x=0.8))
plot_single_profile(0.05, 0.45, prof, soil_depth = 1.0, timestep = "Example")
}
